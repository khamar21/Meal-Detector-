/// Base class for local data sources (e.g., databases, shared preferences).
abstract class LocalDataSource {
  const LocalDataSource();
}

/// Base class for remote data sources (e.g., APIs).
abstract class RemoteDataSource {
  const RemoteDataSource();
}

